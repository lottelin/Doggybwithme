<div class="heading">
  <h1> Dogs Ready For<br>Hangout </h1>
</div>

<div class="scroll-left">
      <p class="scroll-text">Black Friday Special Promotion! Ends TODAY at 7 p.m.! All dogs 20% off!</p>
</div>

<div class="card-container">
  <% @dogs.each do |dog| %>
  <div class="card-trip">
    <%= cl_image_tag dog.photo %>
    <div class="card-trip-infos">
      <div>

        <div class= 'd-flex position-relative'>
        <h2 class= 'woof-hover'><%= link_to dog.name, dog_path(dog) %></h2>
         <%= image_tag "woof.png", alt: "woof", class:'woof' %>
        </div>

        <span><%= dog.breed %>  <%= dog.sex %>  <%= dog.age %></span>
        <div class="dog-traits">
          <div class="dog-characteristics">
            <p class="mr-4">Cuddly&nbsp;<% dog.cuddly.to_i.times do  %>
              <i class="fas fa-paw mr-1"></i>
            <% end %>
            </p>
            <p>Energetic&nbsp;<% dog.energetic.to_i.times do %>
              <i class="fas fa-paw mr-1"></i>
              <% end %>
            </p>
          </div>
          <div class="dog-characteristics">
            <p>Obedient&nbsp; <% dog.obedient.to_i.times do  %>
              <i class="fas fa-paw mr-1"></i>
            <% end %>
            </p>
            <p>Couchpotato&nbsp; <% dog.couchpotato.to_i.times do %>
              <i class="fas fa-paw mr-1"></i>
            <% end %>
            </p>
          </div>
        </div>
      </div>
      <p class="dog-price">$<%= dog.price %></p>
    </div>
  </div>
  <% end %>


</div>
<%#= link_to "Add your  dog", new_new_path %>

